---
type: map
mapping:
    "extraMounts":
        type: seq
        sequence:
            - type: map
              mapping:
                  "device": { type: str, required: yes }
                  "fs": { type: str }
                  "mountPoint": { type: str, required: yes }
                  "options": { type: str }
    "extraMountsEfi":
        type: seq
        sequence:
            - type: map
              mapping:
                  "device": { type: str, required: yes }
                  "fs": { type: str }
                  "mountPoint": { type: str, required: yes }
                  "options": { type: str }
