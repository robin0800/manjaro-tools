---
type: map
mapping:
    requirements:
          type: seq
          sequence:
            - type: map
              mapping:
                "name": { type: str, required: yes }
                "mode": { type: str, required: yes }
    keyrings:
        type: seq
        sequence:
          - type: str
