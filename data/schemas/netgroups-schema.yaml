%YAML 1.2
---
type: seq
sequence:
  - type: map
    mapping:
      "name": { type: str }
      "description": { type: str }
      "selected": { type: bool, default: false }
      "hidden": { type: bool, default: false }
      "packages":
          type: seq
          sequence:
            - type: str
